/* TODO */

#include "shell.h"

/*
 * TODO
 */
int
shell_loop()
{
    while(1)
    {
        
        /*
         * Read input from keyboard
         * If no input, then loop again
         * We have input - parse the command
         * If valid command, use the corresponding device driver to get data
         * Print the data to the screen using terminal driver
         */
    }

    return 0;
}
